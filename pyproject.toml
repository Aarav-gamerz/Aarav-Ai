[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mythic-ai"
version = "1.0.0"
dependencies = [
    "flask",
    "requests",
    "werkzeug",
    "gunicorn"
]

[tool.vercel]
entrypoint = "ai_chat:app"
